mlx>=0.32.0
mlx-vlm==0.6.9
transformers==5.14.1
Pillow>=10.3,<16
numpy>=1.26
safetensors>=0.4
huggingface_hub>=0.30

# The H3 port and still-image conditioning use MLX-VLM plus the direct PIL/NumPy
# Transformers processor. These versions are bounded because the image path uses
# Qwen2VLImageProcessorPil and the Qwen3-VL MLX model layout directly.
# Validation only — the port itself needs neither torch nor diffusers.
# torch>=2.5
# git+https://github.com/huggingface/diffusers@minimax-h3
